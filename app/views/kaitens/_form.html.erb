<%= form_for(@kaiten) do |f| %>
  <% if @kaiten.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kaiten.errors.count, "error") %> prohibited this kaiten from being saved:</h2>

      <ul>
      <% @kaiten.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
